VITE_USE_MOCK=false
VITE_API_URL=https://memory.firm.kiev.ua/api/v1/

# Comma-separated API prefixes to force mock/real in mixed mode.
# Prefixes should match request paths without leading slash.
# Example: api/student,api/payments/refund
VITE_MOCK_ONLY=
VITE_REAL_ONLY=
